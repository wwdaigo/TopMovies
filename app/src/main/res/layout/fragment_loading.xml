<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/demi_white"
    tools:context="io.wwdaigo.topmovies.features.movielist.fragments.LoadingFragment">

    <ProgressBar
        android:id="@+id/loading_progress_bar"
        android:layout_centerInParent="true"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

    <TextView
        android:id="@+id/loading_text_view"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/loading_text"
        android:layout_below="@id/loading_progress_bar"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="@dimen/default_margin"
        android:textSize="@dimen/loading_text_size"
        android:textColor="@color/loading_text"
        android:gravity="center"/>

</RelativeLayout>
